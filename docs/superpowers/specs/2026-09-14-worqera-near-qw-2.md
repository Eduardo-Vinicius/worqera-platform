# Worqera Near-QW Pack 2 — Design

**Date:** 2026-09-14 · **Approved:** chat (“ok faca”)

1. TOP-07 — order templates in localStorage on `/pedidos/novo`
2. Kanban header search by code
3. Drop `lib/setores` as label source (API only; keep MAX_FOTOS)
4. Auto refresh JWT on 401 in `apiV1` + `apiService`
5. `clientId` filter on `listOrders` + consultas clientes
